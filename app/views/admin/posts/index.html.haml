%h1 Blog Posts

= link_to "New Post", new_admin_post_path()

%section.posts
  - @posts.each do |post|
    %div[post]
      .title= link_to post.title, ui_link("/post/" + post.id)
      .state= post.state
      .actions
        = link_to "Edit", edit_admin_post_path(post)
        - if post.draft?
          = link_to "Publish", publish_admin_post_path(post), method: :post
